#include <stdio.h>
#include <math.h>
#include <stdlib.h>


int main()
{
  float x = 45;
  
  //x = sqrt(x); // square root
  //x = pow(x, 3);  // power of 
  //x = round(x);  // round of
  //x = ceil(x);  // ceiling rounded UP
  //x = floor(x);  // floor rounded DOWN
  //x = abs(x);   // absolute value
  //x = log(x);  // finds logarithma
  //x = sin(x);  //sine
  //x = cos(x);  // cosine
  //x = tan(x);  // tangent
  printf("%f", x);

  return 0;
}
